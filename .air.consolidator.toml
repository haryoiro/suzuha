root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/suzuha-consolidator"
  cmd = "CGO_ENABLED=1 go build -buildvcs=false -tags fts5 -o ./tmp/suzuha-consolidator ./cmd/suzuha-consolidator"
  delay = 1000
  exclude_dir = ["tmp", "docs", "vendor", ".git"]
  exclude_regex = ["_test\\.go$"]
  include_ext = ["go", "yaml"]
  include_dir = ["cmd/suzuha-consolidator", "internal"]
  kill_delay = "1s"
  send_interrupt = true
  stop_on_error = true

[log]
  time = false

[misc]
  clean_on_exit = true
